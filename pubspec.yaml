name: italian_tax_identification_number
description: a package to verify, calculate and reverse italian tax identification numbers (Codice Fiscale)
version: 0.0.4
repository: https://github.com/VittorioParagallo/italian_tax_identification_number

environment:
  sdk: ^3.0.6

dependencies:
  dartz: ^0.10.1


dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
